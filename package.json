{
  "name": "vacation-split-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for VacationSplit",
  "engines": {
    "node": "20.18.1"
  },
  "scripts": {
    "build": "npm run build:prod",
    "build:prod": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  }
}
