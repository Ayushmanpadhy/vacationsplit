{
  "name": "vacation-split-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for VacationSplit",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build:prod",
    "build:prod": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "cd backend && npm start"
  }
}
